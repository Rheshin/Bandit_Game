Connect to the machine
list files
You can see an executable
use (file name_of_file) to check the type of the file
execute the file 
execute the file and add as an argument "cat /etc/bandit_pass/bandit20" to get the passwd of the next room

