Connect to the machine
list files
there's an executable that takes a port number as input, and connect to localhost:port_number.
then if you give the password of bandit20, you get the passwd of bandit21 :)


