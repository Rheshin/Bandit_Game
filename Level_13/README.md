First, connect onto the machine
List files
We can see that there is a private ssh key in ~/
named "sshkey.private"
Use scp to get it 
Once u have the ssh priv key, use it to connect to the bandit14 room       
(don't forget to change the perms on the key, it must be 600, can't be higher, or ssh will ignore it)
